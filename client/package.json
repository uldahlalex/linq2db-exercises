{
  "name": "bun-react-template",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run generateapi && bun --hot src/index.ts",
    "build": "bun build ./src/index.html --outdir=dist --sourcemap --target=browser --minify --define:process.env.NODE_ENV='\"production\"' --env='BUN_PUBLIC_*'",
    "start": "NODE_ENV=production bun src/index.ts",
    "generateapi": "bunx swagger-typescript-api generate --path http://localhost:5234/swagger/v1/swagger.json --clean-output --output ./src/api --unwrap-response-data --extract-request-params"
  },
  "dependencies": {
    "react": "^19",
    "react-dom": "^19",
    "react-hot-toast": "^2.6.0"
  },
  "devDependencies": {
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "@types/bun": "latest"
  }
}
